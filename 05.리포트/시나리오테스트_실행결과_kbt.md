# 시나리오 테스트 실행 결과 (kbt, 프론트엔드 프로토타입 기준)

**실행일**: 2026-08-09
**대상**: `src/frontend/`(정적 jQuery 프로토타입, W4 반영본 — mTRM 등록 팝업/좌측 메뉴 정합화/기술동향 등록 팝업/디자인 핸드오프 정합화 포함)
**근거 문서**: `04.검수문서/테스트시나리오_kbt.md`(TC-001~TC-058)
**실행 방법**: Playwright(Chromium, headless)로 프로토타입 화면(`src/frontend/index.html`)을 구동하여 화면 렌더링/내비게이션/모달 노출 등 UI 관측 가능 항목을 자동 점검

> 본 문서는 산출물 Document Chain(17단계)에 포함되지 않는 온디맨드 참고 자료입니다(CLAUDE.md "05.리포트/" 규칙). NotebookLM 등록 대상이 아니며, `.progress.md`/Gate-Check 추적 대상도 아닙니다.
> 공식 산출물 `04.검수문서/테스트결과보고서.md`(#16, Document Chain 대상)는 실 서버/DB/I-F 연동이 준비된 이후 별도로 작성합니다.

---

## 1. 실행 범위 및 한계

현재 구현 상태는 **저장/검증 로직이 없는 정적 프론트엔드 목업**입니다. 조회 버튼(`run-search`)은 no-op이며, 모든 등록/수정 팝업의 확인 버튼은 `close-modal`만 수행합니다(실제 저장·유효성 검증 없음). 이 때문에 테스트시나리오_kbt.md 58건 중:

- **34건**: 화면 렌더링·내비게이션·모달 노출·상세 오버레이 등 **UI 레벨에서 실행 가능** → 이번 회차에 실행
- **28건**: 필수값 검증/저장 차단, I/F·DB 연동, SSO, 메일 발송, 파일 업로드 검증, 성능·보안(OWASP) 등 **백엔드/운영 환경이 필요**하여 이번 회차에서는 **SKIP**(미구현 사유 명시)

---

## 2. 실행 결과 요약

| 구분 | 건수 |
|---|---|
| PASS | 34 |
| FAIL | 0 |
| SKIP(백엔드 의존, 실행 불가) | 28 |
| **합계** | **62**(원본 TC 58건 + 화면별 등록 팝업 노출 스모크 4건 보강) |

---

## 3. PASS 상세

| TC-ID | 항목 | 결과 상세 |
|---|---|---|
| TC-054 | 좌측 GNB에 DASH-AUTO/DASH-MTRM 미노출 | GNB HTML에 두 화면 링크 없음 확인 |
| TC-001 | SCR-001 부품 공정 자동화 현황 접속 | 정상 렌더링(콘텐츠 11,035자) |
| TC-005 | SCR-002 표준공정 마스터 관리 접속 | 정상 렌더링 |
| TC-009 | SCR-003 모듈 공정 자동화 현황 접속 | 정상 렌더링(콘텐츠 10,254자) |
| TC-013 | SCR-004 모듈 표준 작업명 관리 접속 | 정상 렌더링 |
| TC-045 | SCR-005 I/F 실행결과 관리 접속 | 정상 렌더링(성공/실패 이력 표출) |
| TC-053 | MTRM-MAIN 메인 대시보드 접속 | 정상 렌더링(KPI/미니간트/Tech PR/기술동향 미리보기 포함) |
| TC-019 | SCR-006 통합 로드맵 접속 | 정상 렌더링 |
| TC-021 | SCR-007 상세과제 로드맵 관리 접속 | 정상 렌더링 |
| TC-023 | SCR-008 통합 로드맵 차트 접속 + 요약/전체 전환 + 항목 클릭 상세팝업 | 전환 버튼 동작 확인, 간트 바 클릭 시 상세 팝업(`ganttDetail`) 노출 |
| TC-025 | SCR-009 Tech PR 그리드 조회 + 카드 5건 전체 상세 오버레이 노출 | 카드 5건 모두 오버레이 정상 노출 |
| TC-058 | SCR-009 자료유형별(동영상/파일/텍스트/복합자료) 상세 오버레이 렌더링 | 4개 유형 포함 카드 5건 전부 정상 렌더링 |
| TC-029 | SCR-010 Tech PR 관리자 접속 | 정상 렌더링 |
| TC-031 | SCR-011 mTRM 협의체 접속 | 정상 렌더링 |
| TC-033 | SCR-012 mTRM 관리 접속 | 정상 렌더링 |
| TC-035 | SCR-013 기술과제 관리 접속 | 정상 렌더링 |
| TC-037 | SCR-014 기술동향 카드형 목록 조회 + 이전/다음 이동 | 카드 4건, 다음 이동 정상 동작 |
| TC-038 | 기술동향 첫/마지막 항목 이전/다음 버튼 비활성화 | 첫 항목 "이전", 마지막 항목 "다음" 버튼 모두 비활성 확인 |
| TC-047(kbt) | SCR-003 "총 성인화(실적/계획)" 단일 행 표기 | 요약현황 영역에 연도+실적/계획 병기 행 확인 |
| TC-048(kbt) | SCR-003 "+ 작업내용 등록" 버튼 라벨 및 등록 팝업 노출 | 버튼 라벨 일치, 클릭 시 등록 팝업 정상 오픈 |
| TC-052(kbt) | FLOW-MAP 화면 카드 클릭 시 화면 이동(`?showFlowMap=1`) | FLOW-MAP 진입 후 카드 클릭 시 대상 화면(SCR-001)으로 정상 이동 |
| TC-055(kbt2) | SCR-006 하위 SCR-007/008 서브메뉴 접기/펼치기 | 접힘/펼침 모두 정상 동작(GNB 영역 한정 검증) |
| TC-056(kbt2) | SCR-012 "+ mTRM 등록" 모달 필수값(mTRM명/분과/상태/등록일) | 4개 필드 모두 필수(*) 표시 확인 |
| TC-057(kbt2) | SCR-014 "+ 등록" 모달 필수값(제목/구분/내용)·선택값(태그/첨부파일) | 필수 3건/선택 2건 구분 정상 |
| TC-003 | SCR-001 "+ 공정 등록" 팝업 노출 | 정상 |
| TC-005(보강) | SCR-002 "+ 표준공정 등록" 팝업 노출 | 정상 |
| TC-013(보강) | SCR-004 "+ 표준 작업명 등록" 팝업 노출 | 정상 |
| TC-019(보강) | SCR-006 "+ 로드맵 등록" 팝업 노출 | 정상 |
| TC-027 | SCR-009 "기술 문의/제안" 팝업 노출 | 정상 |
| TC-029(보강) | SCR-010 "+ 자료 등록" 팝업 노출 | 정상 |
| TC-031(보강) | SCR-011 "+ 협의체 등록" 팝업 노출 | 정상 |

---

## 4. SKIP 상세(사유)

| TC-ID | 항목 | SKIP 사유 |
|---|---|---|
| TC-002 | 조회조건 미선택 안내 메시지(F-001) | 조회 버튼이 `run-search` no-op(실 연동 시 API 호출로 교체 예정 주석 확인), 안내 메시지 로직 미구현 |
| TC-004 | 필수값 누락 시 저장 차단(F-002) | 모달 확인 버튼이 `close-modal`만 수행, 저장/검증 로직 없음 |
| TC-006 | 중복 공정명 등록 경고(F-003) | 저장/중복검증 로직 미구현 |
| TC-007 | 표준공정 조회·엑셀 다운로드(F-004) | 다운로드/파일 생성 로직 미구현 |
| TC-008 | 조회결과 없음 시 다운로드 버튼 비활성화(F-004) | 조회 실행/빈 결과 상태 로직 미구현 |
| TC-010 | 조회조건 미선택 안내(F-005) | 조회 로직 미구현(mock 데이터 항상 표출) |
| TC-011/012 | I/F 연동-가공테이블 반영/정합성 알림(F-006) | DB/백엔드 연동 필요 |
| TC-014 | 연동중 작업명 삭제 경고(F-007) | 삭제 확인 모달은 정적 텍스트만 노출, 실제 연동 여부 판단 로직 없음 |
| TC-016 | 표준 작업명 마스터 미등록 안내(F-008) | mock 데이터 항상 존재, 빈 상태 시나리오 데이터 미준비 |
| TC-017/018 | I/F 실행/재시도/알림(F-009) | 백엔드 배치·알림 시스템 필요 |
| TC-020 | 기간 역전 오류 안내(F-010) | 저장/검증 로직 미구현 |
| TC-022 | 상위 로드맵 미지정 시 저장 차단(F-011) | 저장/검증 로직 미구현 |
| TC-024 | 조회결과 없음 안내(F-012) | 조회 로직 미구현 |
| TC-026 | 미지원 동영상 포맷 안내(F-013) | 실제 미디어 재생기 미구현(정적 썸네일/재생버튼) |
| TC-028 | 담당자 미등록 발송 불가 안내(F-014) | 메일 발송 로직 미구현 |
| TC-030 | 허용 확장자 외 업로드 차단(F-015) | 첨부 검증 로직 미구현(정적 file-drop) |
| TC-032 | 분과장 미지정 저장 차단(F-016) | 저장/검증 로직 미구현 |
| TC-034 | 권한 없는 사용자 CRUD 차단(F-017) | 로그인/권한 체계 미구현(단일 세션 목업) |
| TC-035/036 | 기술과제-mTRM 자동 매핑/보류(F-018) | 매핑 로직·DB 연계 미구현(SCR-013 정적 mock) |
| TC-038(등록차단) | 제목 누락 저장 차단(F-019) | 저장/검증 로직 미구현(이전/다음 버튼 비활성화 부분은 실행·PASS 완료) |
| TC-039 | 응답 3초 이내(NFR-001) | 정적 파일, 운영 유사 환경 필요 |
| TC-040 | OWASP/권한체크(NFR-002) | 실 서버·인증 체계 필요 |
| TC-041 | 첨부 확장자 차단/이력(NFR-003) | 업로드/이력 로직 미구현 |
| TC-042 | 3브라우저/다국어(NFR-004) | 다국어 전환 UI 미구현(한국어 고정) |
| TC-043 | SSO 미인증 차단(NFR-005) | SSO 연계 미구현 |
| TC-044 | 장애대응 2시간(NFR-006) | 운영 프로세스 문서 검토 사항, UI 테스트 대상 아님 |
| TC-046 | 원본-가공 테이블 정합성(NFR-008) | DB/배치 연계 필요 |
| TC-049/050/051 | DASH-AUTO/DASH-MTRM 대시보드(kbt) | TC-054에서 확인된 대로 현재 GNB 내비게이션에서 제외(설계 확정)되어 화면 자체 접근 불가 |

---

## 5. 결론 및 다음 단계

- 현재 반영된 화면(W4 프론트엔드 프로토타입) 기준으로 **관측 가능한 UI 동작은 전수 PASS**이며, 이번 회차에서 발견된 결함(FAIL)은 없습니다.
- SKIP 28건은 결함이 아니라 **프로토타입 단계의 의도된 범위 제한**(저장/검증/연동 로직 미구현)입니다. 실 개발 착수(Java Spring 백엔드, Oracle 19.0 연동) 이후 재실행이 필요합니다.
- 공식 `04.검수문서/테스트결과보고서.md`(#16)는 Gate-Check 규칙상 `tests/` 코드 존재 + #15 완료를 전제로 하므로, 실 서버 연동 이후 이 SKIP 항목들을 재검증하여 별도로 작성·등록합니다.
- Playwright 자동화 스크립트는 세션 스크래치패드에 보관했습니다(재사용 시 요청 바랍니다).

## 참고 문서

- `04.검수문서/테스트시나리오_kbt.md`
- `src/frontend/index.html`, `js/app.js`, `js/data.js`, `js/render.js`
