# PLM 생기포털 프론트엔드 — 디자인(Claude Design) 대 구현 UI 정합화 수정 요청서

**작성 기준**: `design_handoff_생기포털/source/부품 공정 자동화 현황.dc.html` (Tailwind CSS 마크업, 사이즈 클래스 미지정 요소는 브라우저 기본값 16px로 렌더링됨) vs 현재 구현(`src/frontend/css/common.css`, `src/frontend/js/render.js`)

**범위**: 데이터/인터랙션은 이미 검증 완료 상태이며 이 문서에서 다시 다루지 않습니다. 아래는 **순수 시각적/구조적 CSS 차이**(폰트 크기, 테이블 보더, 패딩, radius/그림자, 카드 컨테이너 유무 등)만 정리한 것입니다. 아직 코드는 수정하지 않았습니다.

---

## A. 전역(공통) 이슈 — common.css 한 곳을 고치면 여러 화면에 동시 적용됨

| # | 항목 | 디자인 값 | 현재 값 | 근거(위치) |
|---|------|-----------|---------|-----------|
| A1 | 기본/테이블 본문 폰트 크기 | 크기 클래스 미지정 → **16px** (거의 모든 화면 테이블 본문) | `html,body{font-size:13px}`, `table.data-table{font-size:13px}`, `table.data-table th{font-size:12px}` | common.css:83,191,193 |
| A2 | 섹션 타이틀(`.section-title`) | `h2.text-sm font-bold` = **14px** (SCR-001, SCR-003 등에서 확인) | `.section-title{font-size:16px}` | common.css:151 |
| A3 | KPI 카드 숫자(`.kpi-value`) | `text-2xl font-bold` = **24px** (SCR-006 l.627-643, MTRM-MAIN l.2111-2127) | `.kpi-value{font-size:28px}` | common.css:231 |
| A4 | 카드/패널 컨테이너 radius+그림자 | `rounded-lg`(**8px**) + `shadow-sm` + `bg-white` + `border` — 필터바/테이블/KPI그리드 등 거의 모든 섹션을 이렇게 감쌈 | `.panel{border-radius:4px}`, **box-shadow 없음** | common.css:234 |
| A5 | **카드 컨테이너 자체가 누락된 화면** | 위 A4 컨테이너로 필터바·테이블이 항상 감싸져 있음 | SCR-002/004/005/009/010/011/012/014의 필터바·테이블이 `.panel` 없이 배경·테두리·그림자·radius **전혀 없이** 맨몸으로 렌더링됨 (`renderTable()`이 래퍼를 안 씌움, 필터바는 `<div style="display:flex...">`만 사용) | render.js 각 `renderSCR0XX` 함수 |
| A6 | 셀 패딩 | 화면마다 두 갈래: SCR-001 계열은 `p-2`=**8px** 균일, SCR-006/007/009~014 계열은 `p-3`=**12px** 균일 | `padding:10px 16px` (상하 10px/좌우 16px, 비대칭) — 모든 화면 동일 | common.css:193,196 |

> A5는 CSS만으로 못 고칩니다 — 화면별 render.js 마크업에 `.panel` 래퍼(또는 동등한 클래스)를 씌우는 작업이 필요합니다.

---

## B. 화면별 개별 이슈

### SCR-001 부품 공정 자동화 현황
- **테이블 격자 보더 없음**: 디자인은 `border-collapse`+모든 `<th>/<td>`에 `border border-gray-200`(가로+세로+외곽 전체 격자) vs 현재 `border-bottom`만 있어 세로선·외곽선이 아예 없음.
- **"협의必(ROI...)" 등 특이사항 셀**: 디자인은 배지(pill)가 아니라 **셀 자체**에 `bg-amber-100 rounded` 배경을 채우고 본문과 동일 크기(16px) 텍스트 사용 vs 현재는 12px 알약형 `.badge`를 셀 안에 삽입.
- (참고, 우선순위 낮음) 폰트 패밀리: 디자인은 시스템 폰트 스택만 사용(웹폰트 미지정) vs 현재 Google Fonts "Noto Sans KR" 명시 로드 — index.html 주석상 임시 조치로 보임.

### SCR-002 표준공정 마스터 관리
- 테이블 격자 보더 없음 (SCR-001과 동일 패턴).
- 표준공정명 셀 배경 누락: 디자인 `td.bg-gray-50` vs 현재 배경 없음(`font-weight:600`만).
- A5(카드 컨테이너 누락) 해당.

### SCR-003 모듈 공정 자동화 현황
- 테이블 격자 보더 없음 (SCR-001과 동일 패턴).
- 동기화 배지: 디자인은 순수 텍스트(`text-gray-400 text-[11px]`, 필 없음) vs 현재 초록색 pill(`.filter-sync-badge`) — 색상·형태 모두 불일치.
- 경고 배너: 디자인 `bg-amber-50 border border-amber-200 rounded-lg`(사방 테두리) vs 현재 좌측 4px 액센트만 있는 회색 계열 배너 — 색 테마·보더 형태 모두 다름.
- 요약표 라벨열/상세표 작업내용열 셀 배경(`bg-gray-50`) 누락.
- 요약표 Best Practice 헤더, 상세표 하위헤더에 현재 구현이 디자인에 없는 강조색(호박/파랑 배경)을 임의로 추가한 상태 — 과잉 강조이므로 제거 필요.

### SCR-004 모듈 표준 작업명 관리
- 테이블 보더는 이미 디자인과 일치(가로선만, 세로/외곽선 없음 — 수정 불필요).
- A5(카드 컨테이너 누락) 해당.
- 셀 패딩 목표값은 `p-3`=12px.

### SCR-005 I/F 실행결과 관리
- 테이블 보더는 이미 디자인과 일치(수정 불필요).
- 결과 배지(성공/실패)는 이미 디자인과 구조 일치(수정 불필요).
- A5(카드 컨테이너 누락) 해당.

### SCR-006 통합 로드맵
- 셀 패딩 목표값 `p-3`=12px 균일.
- 상태 배지 패딩 근소 차이(디자인 `py-0.5`=2px vs 현재 `.badge` 3px) — 우선순위 낮음.

### SCR-007 상세과제 로드맵 관리
- 셀 패딩 목표값 `p-3`=12px 균일. 그 외 고유 이슈 없음.

### SCR-008 통합 로드맵 차트
- **세로 그리드 보더 누락**: 디자인은 로드맵명 열과 분기 그리드 경계, 분기 헤더 컬럼마다 `border-r border-gray-200` 명시 vs 현재 어디에도 세로 구분선 없음.
- 연도 헤더('26년/'27년/'28년) 폰트: 디자인 미지정→16px vs 현재 th 기본 12px 상속.
- 분기 서브헤더 셀 패딩: 디자인 `py-2 px-1`=8px/4px vs 현재 10px/16px 상속 — 분기 컬럼이 디자인보다 훨씬 넓게 렌더링됨.
- 서브태스크 상태 배지 폰트: 디자인 `text-[10px]`(서브 행 전용 축소) vs 현재 `.badge` 12px 고정.
- 막대 높이/색상(상위 24px, 서브 10px)은 이미 일치 — 수정 불필요.

### MTRM-MAIN 메인 대시보드
- 대시보드 간트 표 셀 패딩: 디자인 `p-2`=8px 균일 vs 현재 10px/16px 상속.
- 분기 헤더 폰트: 현재 `font-size:11px`로 인라인 고정되어 있는데, 이는 디자인 목표(12px, 표 자체 `text-xs` 상속)도 현재 공통 기본값(13px)도 아닌 임의의 중간값 — 정리 필요.
- 서브태스크 상태 배지: SCR-008과 동일하게 `text-[10px]` 목표 vs 현재 12px 고정.
- KPI 숫자 폰트는 A3과 동일 이슈, 막대 높이는 이미 일치.

### SCR-009 생산기술 Tech PR
- **카드 썸네일 종횡비 오류**: 디자인은 모든 카드 유형(동영상/파일/텍스트/복합자료)이 `aspect-square`(1:1)인데, 현재 `.media-thumb` 기본값이 `aspect-ratio:16/9`이고 이 화면에서 override하지 않아 **카드가 정사각이 아니라 와이드로 렌더링됨**(가장 눈에 띄는 시각적 버그).
- 카드 그리드 gap: 디자인 `gap-4`=16px vs 현재 `.card-grid-5{gap:20px}`.
- 카드 body 패딩: 디자인 `p-3`=12px vs 현재 `.media-body{padding:16px}`.
- 자료유형 배지: 디자인 `text-[10px]` 사각형(4px radius) vs 현재 `.tech-pr-type-badge{font-size:9px}` 필(pill, 9px radius) — 폰트 크기·모양 모두 다름.
- 카드 제목/메타: 디자인 제목 미지정(16px)/메타 `text-[11px]` vs 현재 제목 15px/메타 12px.
- A5(카드 컨테이너 누락, 필터바 영역) 해당.

### SCR-010 생산기술 Tech PR 관리자
- A5(카드 컨테이너 누락) 해당. 그 외 전역 패턴(A1/A6) 외 고유 이슈 없음.

### SCR-011 mTRM 협의체
- A5(카드 컨테이너 누락) 해당. 상태/메일상태 배지는 이미 디자인과 일치(pill 구조 동일, 수정 불필요). 그 외 고유 이슈 없음.

### SCR-012 mTRM 관리
- A5(카드 컨테이너 누락) 해당. 그 외 고유 이슈 없음.

### SCR-013 기술과제 관리
- 매핑결과 테이블: 디자인은 `border border-gray-200 rounded overflow-hidden`로 얇은 테두리+라운드 처리 vs 현재 감싸는 테두리/라운드 없음.
- 폼 패널: 디자인 `rounded-lg shadow-sm`(8px+그림자) vs 현재 `.form-panel{border-radius:4px}` 그림자 없음(패딩 24px는 이미 일치).

### SCR-014 기술동향
- 목록 카드: SCR-009와 동일 패턴 — 그리드 gap(16px 목표 vs 현재 20px), 카드 radius+그림자(8px+shadow 목표 vs 현재 4px 그림자없음), body 패딩(12px 목표 vs 현재 16px), 제목(16px 목표 vs 현재 15px), 메타(11px 목표 vs 현재 12px).
- **단, 이 화면의 aspect-square는 이미 인라인으로 올바르게 처리되어 있어 SCR-009와 달리 종횡비 버그 없음**.
- 상세뷰(제목 20px/이미지 480px/재생아이콘 44px/선택 라디오 26px)는 디자인 값과 정확히 일치 — 수정 불필요.

---

## 참고: 이미 디자인과 일치해서 손댈 필요 없는 항목
- SCR-004/005/006/007/009~014 테이블의 가로줄만 있는 보더 스타일(세로선 없음)은 이미 디자인과 일치 — **SCR-001/002/003만 풀그리드 보더로 바꾸면 됨**, 다른 화면까지 풀그리드로 바꾸면 오히려 디자인과 어긋납니다.
- mTRM 협의체/관리, I/F 실행결과의 상태 배지(pill) 구조는 이미 일치.
- SCR-008/MTRM-MAIN의 간트 막대 높이·색상은 이미 일치.
- SCR-014 상세뷰 레이아웃은 이미 정확히 일치.
